# AI 量化交易项目 - 评估与开发计划

> 评估日期：2026-03-11  
> 项目路径：`/root/.openclaw/workspace/ai-quant`

---

## 一、项目评估总结

### 1.1 项目整体架构

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **前端** | React 18 + TypeScript + Vite + Ant Design + ECharts | 单页应用，富途牛牛风格深色主题，路由 + 鉴权 + 自选 Context |
| **后端** | Python 3.11 + FastAPI | 模块化 API（auth / stocks / backtest / simulation / risk / watchlist），统一鉴权 |
| **数据层** | SQLite（本地文件） | 三库：`klines.db`（K 线）、`stock_list.db`（股票列表）、`watchlist.db`（自选） |
| **数据源** | JoinQuant（主）+ AkShare（股票列表/个股信息） | K 线优先本地缓存，缺失时拉 JoinQuant；实时行情当前为占位（返回 0） |
| **部署** | PM2 + Nginx + GitHub Actions | 后端需 `PYTHONPATH` 指向项目根目录 |

**架构特点**：
- 前后端分离，前端通过 `/api` 代理访问后端。
- 后端按领域拆分：`api/`（路由）、`services/`（业务）、`models/`（Pydantic）、`strategies/`（策略插件）。
- 数据持久化全部为本地 SQLite，无独立数据库服务，适合单机/小团队。

---

### 1.2 已实现功能评估

| 模块 | 功能 | 完成度 | 备注 |
|------|------|--------|------|
| **鉴权** | 密码登录、Token 校验、前端路由守卫 | ✅ 完整 | 除 `/api/auth/*` 外均需 Bearer token |
| **股票数据** | 列表分页/搜索、历史 K 线、复权、本地缓存 | ✅ 完整 | 列表/K 线均 SQLite + 增量更新 |
| **指标服务** | MA / RSI / MACD / 布林带，K 线图叠加 | ✅ 完整 | `/api/indicators/{symbol}` |
| **回测** | MA 交叉、RSI 策略，滑点/手续费，结果存储与列表/详情/删除 | ✅ 完整 | 策略插件化，结果存 JSON 文件 |
| **自选** | 分组 CRUD、股票增删改、备注、SQLite 持久化 | ✅ 完整 | 前端 WatchlistContext + 分组管理页 |
| **模拟交易** | 下单/撤单、限价/市价、撮合、持仓/订单/成交查询、重置账户 | ✅ 完整 | 内存状态，依赖风控检查 |
| **风控** | 仓位限制、止损止盈、黑名单、交易单位/金额限制、告警 | ✅ 完整 | 内存状态，与模拟交易联动 |
| **实时行情** | 接口存在，批量/单只 | ⚠️ 占位 | 当前返回 price=0 等占位数据，无真实行情 |
| **前端页面** | 自选、分组管理、回测、分析、模拟交易、风控、策略模板、股票详情 | ✅ 完整 | 策略模板为展示/复制用，非可执行配置 |

**结论**：核心链路「数据 → 回测 → 分析」与「自选 → 模拟交易 → 风控」已打通，功能完整度约 **85%**。主要缺口：真实实时行情、后端单元测试覆盖、CI/CD 端到端验证。

---

### 1.3 代码质量与可维护性

| 维度 | 评价 | 说明 |
|------|------|------|
| **分层** | 良好 | 后端 routes → services → models/store；前端 pages/components/services/contexts 清晰 |
| **策略扩展** | 良好 | `BaseStrategy` 抽象 + 注册表，新增策略只需实现接口并 register |
| **配置与环境** | 一般 | 密码、JQ 账号等通过环境变量，缺少 `.env.example` 和文档集中说明 |
| **错误处理** | 一般 | API 层有 HTTPException，部分 service 仅 log 或抛裸 Exception，前端部分接口未统一 toast |
| **类型** | 良好 | 后端 Pydantic，前端 TypeScript，部分 API 返回类型未在 frontend 严格定义 |
| **测试** | 不足 | 仅 `tests/test_*.py` 数个小文件，覆盖回测/存储等，无 API 集成测试、无前端单测 |
| **文档** | 一般 | README 与 TODOLIST 有用，API 有 docstring，缺少接口变更记录与部署故障排查文档 |

---

### 1.4 存在的问题和风险

1. **数据源与稳定性**
   - AkShare 在服务器上易被限流/连接断开（TODOLIST 已记录）。
   - 实时行情为占位，模拟交易「当前价」依赖 `get_realtime_quote`，若一直为 0 则市值/盈亏不真实。
   - JoinQuant 免费账号有日期范围限制（如 2024-11-30～2025-12-07），需在文档和 UI 中说明。

2. **状态与持久化**
   - 模拟交易、风控均为进程内内存，重启后账户/订单/风控规则/告警清零，不利于长期模拟或生产化。

3. **安全与运维**
   - 密码为单一定值环境变量，无多用户、无会话过期策略。
   - 无健康检查深度（仅 `/health` 存活），未检查 DB/数据源可用性。
   - 首次完整 CI/CD 部署尚未跑通，存在部署回归风险。

4. **技术债务**
   - 后端未统一日志格式与请求 ID，不利于排查问题。
   - 前端 `api.ts` 中 `getRealtimeQuotes` 的请求参数为 `symbols`，后端为 query `symbols`，需确认与文档一致（当前一致）。
   - 部分接口返回结构不统一（有的 `data` 包装，有的直接返回字段），建议统一约定。

---

## 二、优先级排序的任务列表

按 **P0（必须）→ P1（重要）→ P2（建议）** 排序，同一优先级内按依赖关系排序。

| 序号 | 优先级 | 任务 | 说明 |
|------|--------|------|------|
| 1 | P0 | 修复/明确实时行情或模拟交易展示逻辑 | 要么接入真实行情（如 JoinQuant 分钟价或其它源），要么在 UI 明确标注「模拟价/占位」并避免误导 |
| 2 | P0 | 完成首次完整 CI/CD 部署测试 | 从 push 到部署到验证接口，确保流程可重复 |
| 3 | P0 | 补充后端关键路径单元测试 | 至少：回测引擎、storage、风控检查、模拟撮合；目标覆盖率可先定 60%+ |
| 4 | P1 | 数据源与稳定性 | AkShare 重试/降级/代理或切数据源；JoinQuant 日期限制在 README 与回测页提示 |
| 5 | P1 | 模拟交易/风控状态持久化 | 将账户、订单、风控规则、告警等落库（SQLite 或现有 DB），重启可恢复 |
| 6 | P1 | 统一 API 响应格式与错误码 | 统一 `{ success, data?, error?, message? }` 及 HTTP 状态码约定 |
| 7 | P1 | 健康检查增强 | `/health` 增加 DB 可读写、数据源可达性（可选） |
| 8 | P2 | 前端单元测试 / E2E | 关键页面与 API 调用单测；E2E 可只做登录+回测一条链路 |
| 9 | P2 | 日志与可观测性 | 请求 ID、结构化日志、可选简单 metrics |
| 10 | P2 | 策略模板与回测参数联动 | 策略模板页可生成/复制配置，并在回测页预填参数（当前为展示用） |
| 11 | P2 | README 与运维文档 | 环境变量清单、常见错误（如 PYTHONPATH、AkShare 超时）、回测日期范围说明 |

---

## 三、分阶段的开发计划

### 阶段一：稳定与可交付（约 2～3 周）

**目标**：CI/CD 可靠、核心路径有测试保障、数据源与行情行为明确。

- **1.1 CI/CD**
  - 完成一次从 main 推送到自动部署的完整流程，并文档化。
  - 确保服务器上 `PYTHONPATH`、依赖、前端 build 产物、Nginx/PM2 配置正确。

- **1.2 测试**
  - 为 `backtest_service`、`storage_service`、`risk_service.check_order`、`simulation_service` 撮合/下单编写/补全单测。
  - CI 中强制 pytest 通过，可设最低覆盖率门槛。

- **1.3 行情与数据源**
  - 方案 A：在现有 JoinQuant 权限内，用分钟/日线最新价作为「模拟实时价」，并在前端标注「模拟」。
  - 方案 B：保持占位，但在模拟交易页明确说明「当前为占位数据，市值/盈亏仅供参考」。
  - 在回测页或 README 中注明 JoinQuant 免费账号的日期范围，避免用户选错区间。

- **1.4 文档**
  - 更新 README：环境变量（如 `QUANT_AUTH_PASSWORD`、`JQ_USERNAME`、`JQ_PASSWORD`）、回测日期限制、部署步骤与常见问题。

**交付物**：CI 绿、单测通过、README 更新、行情/日期限制说明到位。

---

### 阶段二：持久化与体验（约 2～3 周）

**目标**：模拟交易与风控可重启不丢数据，API 与前端体验更一致。

- **2.1 模拟交易持久化**
  - 设计 SQLite 表（或沿用现有 data 目录）：账户快照、订单、成交、持仓。
  - 启动时加载默认账户；下单/撮合时写入 DB；查询从 DB 读。

- **2.2 风控持久化**
  - 风控规则、黑名单、止损止盈配置、告警写入 DB，重启后恢复。

- **2.3 API 规范**
  - 统一响应格式与错误码，中间件或依赖中统一包装。
  - 前端 api 层统一解析 `success/error/message`，并配合 toast/message 展示。

- **2.4 健康检查**
  - `/health` 增加 SQLite 可读写检测；可选：数据源连通性（如 JoinQuant auth）返回在 detail 中，不阻塞 200。

**交付物**：模拟账户与风控重启可恢复、API 规范文档与实现、健康检查增强。

---

### 阶段三：扩展与运维（约 2 周+）

**目标**：便于扩展策略与运维排错，为后续「实盘/更多数据源」打基础。

- **3.1 策略与回测**
  - 策略模板页与回测页参数联动（如选择模板后回测页预填策略 id 与参数）。
  - 可选：更多内置策略（如 MACD 金叉死叉、布林带突破）或策略参数校验。

- **3.2 可观测性**
  - 请求 ID（middleware 生成并写入 response header / log）。
  - 结构化日志（JSON 或键值对），便于 grep/ELK。
  - 可选：简单 metrics（请求数、回测次数、错误数）供 Prometheus 或自研看板拉取。

- **3.3 前端测试**
  - 关键 API 封装与 1～2 个核心页面的单测（如回测配置、分析页数据展示）。
  - 可选：一条 E2E（登录 → 选择股票 → 回测 → 查看分析）。

- **3.4 安全与多用户（可选）**
  - 若需多用户：引入用户表与 session/token 绑定，模拟账户/风控按 user_id 隔离。
  - 会话过期与刷新 token 策略。

**交付物**：策略模板与回测联动、日志与请求 ID、可选前端单测/E2E、安全扩展点文档。

---

## 四、技术债务与改进建议

### 4.1 短期可做

- **统一响应格式**：所有 API 返回 `{ success: bool, data?: T, error?: string, message?: string }`，错误时 HTTP 4xx/5xx + 同一结构。
- **环境变量文档**：在 README 或 `docs/ENV.md` 列出 `QUANT_AUTH_PASSWORD`、`JQ_USERNAME`、`JQ_PASSWORD` 及可选项。
- **回测日期校验**：后端在回测前校验 start/end 是否在数据源有效范围内，超出时返回明确错误提示。
- **前端错误处理**：在 axios 拦截器或 api 封装层统一处理 401（跳转登录）、5xx（toast），避免每处手写。

### 4.2 中期建议

- **配置模块**：用 Pydantic Settings 集中管理环境变量与默认值，避免各 service 散落 `os.getenv`。
- **模拟交易与风控落库**：见阶段二；若未来有多实例需求，可考虑将状态迁到 Redis 或 PostgreSQL。
- **数据源抽象**：将「K 线」「实时价」「股票列表」抽象为 DataProvider 接口，便于切换 AkShare/JoinQuant/其他源而不改业务逻辑。

### 4.3 长期方向

- **实盘/模拟分离**：明确区分「回测」「模拟交易」「实盘」三种模式，权限与风控分级。
- **多用户与权限**：若产品化，需要用户体系、角色与资源隔离。
- **性能**：若 K 线/回测量级增大，可考虑异步任务队列（Celery/RQ）、结果缓存、分页优化。

---

## 五、文档与协作建议

- **TODOLIST.md**：与本文档对齐，将「进行中/待办」按上述优先级打标签（P0/P1/P2），便于 heartbeat 与人类跟进。
- **CHANGELOG 或 Release notes**：版本发布时记录 API 变更、数据源限制、配置项变更，便于回溯。
- **故障排查**：在 `docs/` 下增加「部署与排错」（如 PYTHONPATH、500 错误、AkShare 超时、JoinQuant 日期限制），方便新人与运维。

---

*本文档可根据实际进度在 TODOLIST.md 与迭代中更新。*

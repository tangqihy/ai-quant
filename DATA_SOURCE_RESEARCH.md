# A股实时行情数据源调研

## 需求
- 避免全量获取数据
- 只提供搜索功能和自选股数据
- 比东财和腾讯更稳定的接口

## 调研结果

### 1. 新浪财经 API（推荐）

**优点：**
- 稳定性较好，历史悠久
- 支持批量查询（最多100只）
- 无需注册，直接访问
- 数据格式简单

**接口示例：**
```
https://hq.sinajs.cn/list=sh600519,sz000001
```

**返回格式：**
```javascript
var hq_str_sh600519="贵州茅台,1740.00,1735.00,1745.00,1750.00,1738.00,1744.00,1745.00,10000,17450000,1745.00,100,1744.00,200,...";
```

**限制：**
- 需要处理返回的 JavaScript 变量格式
- 可能有频率限制（未明确说明）
- 需要Referer头（部分场景）

---

### 2. 腾讯财经 API（当前在用）

**优点：**
- 稳定性中等
- 支持批量查询
- 返回JSON格式

**缺点：**
- 有时限流
- 数据字段较少

---

### 3. 东方财富 API（当前在用）

**优点：**
- 数据全面
- 实时性好

**缺点：**
- 服务器IP容易被限流
- 需要处理反爬

---

### 4. 雪球 API

**优点：**
- 数据质量高
- 社区活跃

**缺点：**
- 需要登录/Token
- 有严格的频率限制
- 个人用户难以申请

---

### 5. 聚宽 JoinQuant（当前历史数据在用）

**优点：**
- 数据专业
- API规范

**缺点：**
- 免费账号有日期限制
- 实时数据需要付费
- 仅限历史数据

---

### 6. Tushare

**优点：**
- 数据全面
- 社区支持好

**缺点：**
- 需要注册Token
- 免费版有积分限制
- 实时数据需要付费

---

### 7. Baostock

**优点：**
- 免费
- 数据较全

**缺点：**
- 更新频率较低（日级）
- 不适合实时行情

---

## 推荐方案

### 方案一：新浪财经（首选）

**实现思路：**
1. 后端封装新浪API调用
2. 只查询用户自选的股票（避免全量获取）
3. 添加本地缓存（5-15秒）
4. 失败时降级到腾讯/东财

**代码示例：**
```python
import requests

def get_sina_realtime_quotes(symbols: List[str]) -> List[Dict]:
    """
    获取新浪实时行情
    symbols: 股票代码列表，如 ['sh600519', 'sz000001']
    """
    # 批量查询，最多100只
    url = f"https://hq.sinajs.cn/list={','.join(symbols)}"
    headers = {
        'Referer': 'https://finance.sina.com.cn',
        'User-Agent': 'Mozilla/5.0'
    }
    
    response = requests.get(url, headers=headers, timeout=5)
    # 解析返回的 JavaScript 变量
    # ...
```

---

### 方案二：多源降级策略

**实现思路：**
1. 主源：新浪财经
2. 备用1：腾讯财经
3. 备用2：本地缓存（上次成功数据）
4. 轮询机制，失败自动切换

**架构：**
```
用户请求自选行情
    ↓
尝试新浪API（带5秒缓存）
    ↓ 失败
尝试腾讯API
    ↓ 失败
返回本地缓存数据
    ↓ 无缓存
返回占位数据（price=0）
```

---

### 方案三：WebSocket 长连接（进阶）

**适用场景：**
- 需要高频实时更新
- 用户同时在线数少

**实现：**
- 使用东方财富或同花顺的 WebSocket 接口
- 只订阅用户自选的股票
- 需要处理连接维护和心跳

---

## 实施建议

### 短期（推荐）

采用**方案一：新浪财经**，理由：
1. 稳定性好，历史悠久
2. 无需注册，接入简单
3. 支持批量查询，适合自选场景
4. 添加缓存后压力小

### 实现步骤：

1. **新增数据源模块** `app/services/realtime_service.py`
   - 封装新浪API
   - 添加本地缓存（Redis或内存）
   - 实现降级逻辑

2. **修改前端**
   - Dashboard 自选列表调用新接口
   - 显示数据来源和更新时间

3. **优化策略**
   - 只查询用户自选的股票（避免全量）
   - 缓存5-15秒，减少API调用
   - 后台定时刷新，用户请求直接读缓存

### 代码结构：

```
app/services/
├── realtime_service.py      # 实时行情服务（新增）
│   ├── SinaDataProvider     # 新浪数据源
│   ├── TencentDataProvider  # 腾讯数据源（备用）
│   └── CachedDataProvider   # 缓存包装器
```

---

## 注意事项

1. **合规性**
   - 新浪财经API为公开接口，但需遵守其使用条款
   - 避免高频请求，建议添加缓存

2. **稳定性**
   - 任何免费接口都可能随时变化
   - 建议实现多源降级

3. **数据质量**
   - 免费接口可能有延迟（15秒左右）
   - 对于量化交易，建议使用付费数据服务

4. **服务器部署**
   - 新浪对服务器IP限制较少
   - 但仍建议添加合理的请求间隔

---

## 参考链接

- 新浪财经API文档（非官方）：https://blog.csdn.net/weixin_44517301/article/details/122923549
- Tushare文档：https://tushare.pro/document/2
- 聚宽文档：https://www.joinquant.com/help/api/doc

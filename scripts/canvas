#!/usr/bin/env bash
# Stock Canvas CLI 快捷入口：scripts/canvas <args...>
set -euo pipefail
cd "$(dirname "$0")/.."
# 优先使用项目虚拟环境
if [ -x .venv/bin/python ]; then
  exec .venv/bin/python -m app.cli.canvas_cli "$@"
fi
exec python3 -m app.cli.canvas_cli "$@"
